```javascript
import { Suspense } from 'react';

const g = wrapPromise(getData);

function Child(props) {
  const data = g.read();

  return <div>{ data }</div>;
}

function Demo(props) {
  return (
    <Suspense fallback="Loading...">
      <Child />
    </Suspense>
  );
}
```